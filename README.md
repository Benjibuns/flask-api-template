# Flask API template

## Setup

- Setup virtual environment

```
$ python -m venv venv
```

- install the packages

```
(venv) $ pip install -r requirements.txt
```

## runninf the server

```
(venv) $ flask run
```
